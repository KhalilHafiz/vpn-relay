services:
  - type: web
    name: vpn-relay
    env: docker
    plan: free
    region: frankfurt  # or oregon if Frankfurt is slow
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeploy: true
